// git status
// git add .gitignore 
// git commit -m "Add .gitignore"
// git push origin main 

// git clone <Repository URL>
// git clone --> copy remote repo (like github) to your local system
// git clone --> A new folder is created 
// git clone --> All files are downloaded into that folder


// git diff --> see the (differences) changes between files in your git repo 

// git checkout -f --> Discard all local uncommited changes 
//git checkout -f --> restore files to the last commited state 
//git checkout -f --> Modified files revereted 

// branch created 